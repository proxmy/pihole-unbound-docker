# 🛡️ Instalador Automático: Pi-hole + Unbound

Este script configura una Raspberry Pi desde cero. Actualiza el sistema, instala Docker, y despliega Pi-hole con Unbound automáticamente.

## 🚀 Instalación (1 solo comando)

Copia y pega esto en tu terminal y pulsa Enter:

```bash
wget -O install.sh [https://raw.githubusercontent.com/TU_USUARIO/TU_REPO/main/install.sh](https://raw.githubusercontent.com/TU_USUARIO/TU_REPO/main/install.sh) && sudo bash install.sh
```
